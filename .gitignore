.databricks/
__pycache__/
*.pyc
.DS_Store
node_modules/
.venv/
*.egg-info/
# Build do frontend: não versionado (é artefato). O bundle o envia ao App via
# `sync.include` no databricks.yml, então continua sendo deployado mesmo
# ignorado aqui. Rode `npm run build` em app/frontend antes do deploy (README).
app/frontend/dist/
.env
.env.local
env.local
